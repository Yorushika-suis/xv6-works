1.a0-a7可以保存参数。13存储到了a2寄存器。
2.根据指令可以看出，call.c中调用了f(8)，f(8)调用了g(8)，g(8)返回11。同时，printf的参数为f(8)+1=12，而汇编指令中直接将12作为参数存储在a1中，说明f,g作为内联被优化，其结果直接被计算出，从而不再需要调用它们。
3.printf的地址为(ra)+0x64a=0x67a。
4.ra的值为0x34+4=0x38。
5.He110 World。即使改变为大端，也不需要转换，因为存储和读取的顺序是统一的，存储和读取都倒序完成也能得到正确的结果。